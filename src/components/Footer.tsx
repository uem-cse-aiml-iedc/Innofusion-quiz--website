export function Footer() {
  return (
    <footer className="relative z-10 mt-12 border-t border-border/40 px-4 py-6 text-center text-xs text-muted-foreground">
      <p>⚔️ Startup Clash Quiz — Forged for Entrepreneurs · {new Date().getFullYear()}</p>
    </footer>
  );
}
